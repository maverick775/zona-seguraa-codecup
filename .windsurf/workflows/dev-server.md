---
description: Start the local Next.js development server
---

# Dev Server

// turbo
1. Install dependencies (if needed):
```bash
cd zona-seguraa && npm install
```

// turbo
2. Start the dev server:
```bash
cd zona-seguraa && npm run dev
```

3. Open `http://localhost:3000` in the browser.

4. Verify Supabase connection at `http://localhost:3000/health`.
